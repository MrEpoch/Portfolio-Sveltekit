/* 
import { render, screen, fireEvent } from "@testing-library/svelte";
import { expect, describe, test } from "vitest";
import ProjectCard from "components/projects/project-card.svelte";
import projects from "lib/texts/projects_content";

// For this to start working:
// open file node_modules/@sveltejs/kit/src/runtime/app/environment.js
// In that file comment export building and version, because i don't want to search for what that path is
// In vitest.config.js add alias for node_modules/@sveltejs/kit/src/runtime/app to $app so special imports would work


/// AboutMe.svelte test, checking for greeting header content
//
//describe("Renders about me component", () => {
// test("Shows content", () => {
//
//   render(ProjectCard, {
//     props: { project_data: projects[0] }
//   });
//   expect(screen.getByText("Tomato Dream")).toBeInTheDocument();
// });
  //);
*/
